  spring:
    config:
      import: classpath:secret/db.yml, classpath:secret/portone.yml, classpath:secret/jwt.yml, classpath:secret/oauth.yml
    jpa:
      hibernate:
        ddl-auto: none
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.MariaDBDialect
      open-in-view: false
